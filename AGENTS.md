# AGENTS.md — Agent & Developer Quickstart Guide

> راهنمای جامع و فشرده برای مدل‌های هوش مصنوعی (AI Agents) و توسعه‌دهندگان جهت درک سریع هویت، معماری، قوانین رفتاری و نقشه فنی این مخزن.

---

## ۱. هویت و هدف بنیادین پروژه (Product Identity)

این پروژه یک **«گرامر چکر سنتی» نیست**؛ بلکه یک **دستیار سراسری بیان، رفع سد ذهنی و یادگیری نگارش انگلیسی (System-wide Expression Companion)** است.

### مسئله اصلی کاربر:
کاربر هنگام نگارش متن‌های انگلیسی (در تعامل با ایجنت‌های کدنویسی، پیام‌رسان‌ها، ایمیل یا مستندات فنی)، ممکن است:
1. الگوهای ساختار ذهنی فارسی را به انگلیسی منتقل کند (Persian-to-English cognitive transfer).
2. با انگلیسی دست‌وپا شکسته (Broken English) بنویسد.
3. معادل یک واژه را نداند و آن را به فارسی داخل قلاب قرار دهد (مثال: `Can we [سازگار کنیم] this?`).
4. نگران باشد که مدل‌های هوش مصنوعی متوجه منظور فنی او نشوند یا کدها بر اثر کج‌فهمی بازنویسی شوند (*Semantic Drift*).

### وظیفه دستیار:
* متن سلکت‌شده را با شورتکات سراسری دریافت کند.
* نیت فنی کاربر را محافظه‌کارانه درک کرده و به انگلیسی روان، طبیعی و حرفه‌ای تبدیل کند.
* **چِک‌پوینت معنایی (Semantic Checkpoint):** برداشت خود از منظور کاربر را حتماً به فارسی نشان دهد («💡 منظور: ...») تا کاربر مطمئن شود سوءبرداشتی رخ نداده است.
* با فشردن کلید `Enter` متن جدید جایگزین شود و با `Esc` یا کلیک بیرون بسته شود.
* رویداد را به صورت سایلنت در دیتابیس محلی (SQLite) برای ایجاد پروفایل یادگیری ذخیره کند.

---

## ۲. اصول تغییرناپذیر معماری و محصول (Core Rules & ADRs)

هر ایجنتی که در این پروژه کار می‌کند باید اصول زیر را رعایت کند:

1. **اصلاح کمینه‌گرا در برابر بازنویسی سلیقه‌ای (Minimal Intervention):**
   * ساختار و واژگان کاربر تا جای ممکن حفظ شود؛ فقط اشتباهات گرامری، حروف اضافه و معادل‌های نامناسب اصلاح شوند.
   * نام توابع، متغیرها، APIها، کامندها و کدهای برنامه‌نویسی **هرگز نباید دستکاری شوند**.
   * اگر جمله کاربر درست است، باید `No Change Needed` / تیک سبز داده شود.
2. **قرارداد داده تک‌مرحله‌ای (Single-Inference JSON):**
   کل داده‌های تصحیح، برداشت معنایی فارسی، و دسته‌بندی تغییرات در یک درخواست واحد JSON از مدل دریافت می‌شود.
3. **معماری تفکیک‌شده (Decoupled / Hexagonal Architecture):**
   * **هسته منطقی (Core Backend):** ۱۰۰٪ پایتونی و مستقل از سیستم‌عامل (موتور پرامپت، آداپتورهای AI مثل Gemini Flash / Groq / Ollama، دیتابیس محلی SQLite).
   * **لایه‌های نمایش (UI Adapters):**
     * **اوبونتو (GNOME Wayland):** افزونه سبک کامپوزیتور گنوم جهت نمایش پاپ‌آپ در کنار نشانگر ماوس (`global.get_pointer()`)، بدون پنجره مستقل کلاینت و مصون از تایلینگ Pop-Shell.
     * **ویندوز:** آداپتور بومی Win32 Frameless.
4. **حریم خصوصی محلی (Local-First):**
   سوابق صرفاً در فایل محلی SQLite ذخیره شده و هیچ تله‌متری به سرورهای خارجی ارسال نمی‌شود.

---

## ۳. نقشه راه و اسناد مرجع (Repository Map)

```text
/home/sajjad/Desktop/AI_Enginniering/writing_assistant/
│
├── AGENTS.md                  # راهنمای سریع ایجنت‌ها (همین فایل)
├── README.md                  # معرفی کلی پروژه و راهنمای اجرا
├── pyproject.toml             # پیکربندی پکیج پایتون، وابستگی‌ها و تست‌ها
│
├── src/writing_companion/     # هسته پردازشی ۱۰۰٪ پایتونی (Core Engine)
│   ├── config.py              # تنظیمات کلیدهای API، مدل‌ها و دیتابیس
│   ├── cli.py                 # رابط IPC استاندارد و اجرای مستقیم در ترمینال
│   ├── core/                  # منطق اصلی (موتور پرامپت، اسکیماهای JSON، ارکستراتور)
│   ├── providers/             # آداپتورهای هوش مصنوعی (Groq, Gemini, Ollama, Mock)
│   └── storage/               # مخزن ذخیره‌سازی محلی SQLite با مد WAL
│
├── extension/                 # آداپتور لایه نمایش گنوم ۴۶ (GNOME Shell Extension)
│   ├── extension.js           # چرخه حیات، شورتکات سراسری، اجرای ناهمگام پایتون
│   ├── popup.js               # پاپ‌آپ متصل به ماوس در کامپوزیتور (مصون از Pop-Shell)
│   ├── keyboard.js            # شبیه‌ساز کلید Shift+Insert در دستگاه مجازی Clutter
│   ├── stylesheet.css         # استایل ظاهری پاپ‌آپ (Adwaita Dark)
│   └── schemas/               # کلید میانبر کامپایل‌شده سیستم (Ctrl+Alt+G)
│
├── tests/                     # تست‌های خودکار یکپارچگی و یونیت
│   ├── integration/test_cli_ipc.py
│   └── unit/                  # تست‌های پرامپت، اسکیما و دیتابیس
│
├── specs/                     # مستندات رسمی توسعه مبتنی بر مشخصات (Spec-Kit)
│   └── 001-in-place-popup-assistant/
│       ├── spec.md            # مشخصات رسمی ویژگی اول
│       ├── plan.md            # نقشه پیاده‌سازی
│       ├── data-model.md      # مدل داده‌ها
│       ├── contracts/         # قراردادهای رسمی JSON، SQLite و IPC
│       ├── quickstart.md      # راهنمای اعتبارسنجی دستی
│       └── tasks.md           # لیست ۳۵ تسک پیاده‌سازی‌شده
│
├── docs/                      # اسناد رسمی و کانتکست تاریخی
│   ├── 01_BRD.md              # سند نیازمندی‌های محصول و معماری کلان (v2.2)
│   ├── 02_CONTEXT_AND_EVOLUTION.md # پیشینه کامل گفتگوها، تحولات فکری و کشف گپ‌ها
│   ├── 03_DECISION_LOG.md     # لاگ رسمی تصمیمات معماری (ADR-01 تا ADR-09)
│   ├── 04_TECHNICAL_SPIKE_1.md# گزارش تست اسپایک فنی اول در Wayland
│   └── 05_IN_PLACE_POPUP_PLAN.md# سند تفصیلی معماری پاپ‌آپ در کنار ماوس
│
└── prototype/                 # کدهای نمونه اولیه و اسپایک‌های اولیه
```

---

## ۴. دستورالعمل‌های همکاری برای ایجنت‌ها (Agent Workflow)

* **اجرای تست‌ها:** قبل از هر کامیت مطمئن شوید تست‌ها با دستور `.venv/bin/pytest` همگی سبز هستند (۱۳ تست فعال).
* **همگام‌سازی مستندات:** هر تصمیم فنی جدید باید پیش از اجرا با کاربر مطرح و در `docs/03_DECISION_LOG.md` و `docs/02_CONTEXT_AND_EVOLUTION.md` ثبت شود.
* **کیفیت کامیت‌ها:** پیام‌های کامیت گیت باید استاندارد (Conventional Commits)، منظم و معنادار باشند.
* **آرامش در اجرا:** هیچ کدی بدون هماهنگی و بررسی قبلی با کاربر به صورت شتاب‌زده پیاده‌سازی نشود.

